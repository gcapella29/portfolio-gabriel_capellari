import {resolveProjectAccess} from '@/core/session';
import {readV2Content} from '@/core/onboarding-data';
import {sectionsForSegment} from '@/core/content-schema';
import {RepeatableSections} from '@/app/setup/[slug]/[step]/repeatable-sections';
import {compactPortfolioEnglishRole,portfolioDefaults} from '@/templates/portfolio/native-data';
import {saveContentAction} from '../actions';
import ContentWorkspace,{type ContentNavItem} from './content-workspace';
import styles from './content.module.css';

const v=(o:Record<string,unknown>,k:string)=>String(o[k]??'');
const storedOr=<T,>(content:Record<string,unknown>,key:string,fallback:T)=>Array.isArray(content[key])?content[key] as T:fallback;
const legacyEnglishRoles=new Set(['Poker journalist · Live tournament reporter · SEO writer · Learner .·.','Poker journalist · Live tournament reporter · SEO writer · Learner .\'.']);

function portfolioLists(content:Record<string,unknown>){
 return {...content,
  portfolio_languages:storedOr(content,'portfolio_languages',portfolioDefaults.identity.languages.map(item=>({country:item.country,label_pt:item.pt,label_en:item.en}))),
  portfolio_ticker:storedOr(content,'portfolio_ticker',portfolioDefaults.ticker.map(title=>({title}))),
  portfolio_stats:storedOr(content,'portfolio_stats',portfolioDefaults.stats.map(item=>({number:item.number,label_pt:item.label.pt,label_en:item.label.en}))),
  portfolio_featured_years:storedOr(content,'portfolio_featured_years',portfolioDefaults.featured.years.map(item=>({label_pt:item.pt,label_en:item.en}))),
  portfolio_coverage:storedOr(content,'portfolio_coverage',portfolioDefaults.coverage.map(([event,years])=>({event,years}))),
  portfolio_links:storedOr(content,'portfolio_links',portfolioDefaults.links.map(item=>({name:item.name,description_pt:item.description.pt,description_en:item.description.en,url:item.href}))),
  portfolio_career:storedOr(content,'portfolio_career',portfolioDefaults.career.map(item=>({role_pt:item.role.pt,role_en:item.role.en,years:item.years,organization:item.organization,description_pt:item.description.pt,description_en:item.description.en}))),
  portfolio_education:storedOr(content,'portfolio_education',portfolioDefaults.education.map(([title,institution])=>({title_pt:title.pt,title_en:title.en,institution}))),
  portfolio_skills:storedOr(content,'portfolio_skills',portfolioDefaults.skills.map(item=>({label_pt:item.pt,label_en:item.en}))),
  portfolio_gallery_meta:storedOr(content,'portfolio_gallery_meta',portfolioDefaults.galleryMeta.map(item=>({year:item.year,alt_pt:item.alt.pt,alt_en:item.alt.en})))
 };
}

function EditorSection({id,number,title,summary,open=false,children}:{id:string;number:string;title:string;summary:string;open?:boolean;children:React.ReactNode}){
 return <details className={styles.disclosure} id={id} data-number={number} open={open}><summary><span><strong>{title}</strong><small>{summary}</small></span></summary><div className={styles.sectionBody}>{children}</div></details>;
}

export default async function ContentPage({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{saved?:string}>}){
 const {slug}=await params,{saved}=await searchParams,{project}=await resolveProjectAccess(slug),data=await readV2Content(project.id),portfolio=project.segment==='portfolio',trainer=project.segment==='personal-trainer',definitions=sectionsForSegment(project.segment),previewUrl=`/preview/${encodeURIComponent(project.slug)}`;
 if(portfolio){
  const nav:ContentNavItem[]=[{id:'content-introduction',label:'Abertura e perfil'},...definitions.map(def=>({id:`content-${def.key}`,label:def.label})),{id:'content-contact',label:'Contato'}];
  return <div className="editor-page"><header><span className="eyebrow dark-text">CONTEÚDO</span><h1>Edite o site por seções.</h1><p>Navegue pela estrutura real do portfólio e alterne entre português e inglês sem misturar os campos.</p></header>{saved?<div className="notice success"><strong>Rascunho salvo.</strong> Confira as alterações no Preview antes de publicar.</div>:null}
   <ContentWorkspace slug={project.slug} previewUrl={previewUrl} saved={Boolean(saved)} portfolio nav={nav} action={saveContentAction}>
    <EditorSection id="content-introduction" number="01" title="Abertura e perfil" summary="Nome, localização, apresentação e textos principais" open>
     <div className="form-grid"><label className="field"><span>Nome profissional</span><input name="name" defaultValue={v(data.identity,'name')||project.name}/></label><label className="field"><span>Localização</span><input name="location" defaultValue={v(data.identity,'location')||portfolioDefaults.identity.location}/></label></div>
     <div data-language="pt" className={styles.languageFields}><span className={styles.languageTitle}>🇧🇷 CONTEÚDO EM PORTUGUÊS</span><label className="field"><span>Subtítulo da abertura</span><input name="hero_text" defaultValue={v(data.content,'hero_text')||portfolioDefaults.identity.role.pt}/></label><label className="field"><span>Título da seção Sobre</span><textarea name="hero_title" rows={2} defaultValue={v(data.content,'hero_title')||portfolioDefaults.about.title.pt}/></label><div className="form-grid"><label className="field"><span>Sobre — primeiro parágrafo</span><textarea name="description" rows={5} defaultValue={v(data.identity,'description')||portfolioDefaults.about.paragraphs[0].pt}/></label><label className="field"><span>Sobre — segundo parágrafo</span><textarea name="about" rows={5} defaultValue={v(data.content,'about')||portfolioDefaults.about.paragraphs[1].pt}/></label></div><div className="form-grid"><label className="field"><span>Apresentação publicada</span><textarea name="primary_offer" rows={4} defaultValue={v(data.content,'primary_offer')||portfolioDefaults.profileBio.pt}/></label><label className="field"><span>Cobertura em destaque</span><textarea name="proof" rows={4} defaultValue={v(data.content,'proof')||portfolioDefaults.featured.description.pt}/></label></div></div>
     <div data-language="en" className={styles.languageFields}><span className={styles.languageTitle}>🇬🇧 ENGLISH CONTENT</span><label className="field"><span>Opening subtitle</span><input name="hero_text_en" defaultValue={legacyEnglishRoles.has(v(data.content,'hero_text_en'))?compactPortfolioEnglishRole:v(data.content,'hero_text_en')||portfolioDefaults.identity.role.en}/></label><label className="field"><span>About section title</span><textarea name="hero_title_en" rows={2} defaultValue={v(data.content,'hero_title_en')||portfolioDefaults.about.title.en}/></label><div className="form-grid"><label className="field"><span>About — first paragraph</span><textarea name="description_en" rows={5} defaultValue={v(data.identity,'description_en')||portfolioDefaults.about.paragraphs[0].en}/></label><label className="field"><span>About — second paragraph</span><textarea name="about_en" rows={5} defaultValue={v(data.content,'about_en')||portfolioDefaults.about.paragraphs[1].en}/></label></div><div className="form-grid"><label className="field"><span>Published introduction</span><textarea name="primary_offer_en" rows={4} defaultValue={v(data.content,'primary_offer_en')||portfolioDefaults.profileBio.en}/></label><label className="field"><span>Featured coverage</span><textarea name="proof_en" rows={4} defaultValue={v(data.content,'proof_en')||portfolioDefaults.featured.description.en}/></label></div></div>
    </EditorSection>
    <RepeatableSections definitions={definitions} initial={portfolioLists(data.content)} collapsible/>
    <EditorSection id="content-contact" number={String(definitions.length+2).padStart(2,'0')} title="Contato" summary="Canais, currículo e publicação do Instagram">
     <div className="form-grid"><label className="field"><span>E-mail</span><input name="email" type="email" defaultValue={v(data.contact,'email')||portfolioDefaults.contact.email}/></label><label className="field"><span>E-mail alternativo</span><input name="email_alt" type="email" defaultValue={v(data.contact,'email_alt')||portfolioDefaults.contact.emailAlt}/></label><label className="field"><span>WhatsApp</span><input name="whatsapp" defaultValue={v(data.contact,'whatsapp')||portfolioDefaults.contact.whatsapp}/></label><label className="field"><span>WhatsApp exibido</span><input name="whatsapp_label" defaultValue={v(data.contact,'whatsapp_label')||portfolioDefaults.contact.whatsappLabel}/></label><label className="field"><span>Instagram</span><input name="instagram" defaultValue={v(data.contact,'instagram')||portfolioDefaults.contact.instagram}/></label><label className="field"><span>LinkedIn</span><input name="linkedin" type="url" defaultValue={v(data.contact,'linkedin')||portfolioDefaults.contact.linkedin}/></label><label className="field"><span>Currículo (URL)</span><input name="cv" defaultValue={v(data.contact,'cv')||portfolioDefaults.contact.cv}/></label><label className="field"><span>Publicação do Instagram (URL)</span><input name="reel" type="url" defaultValue={v(data.contact,'reel')||portfolioDefaults.contact.reel}/></label></div>
    </EditorSection>
   </ContentWorkspace>
  </div>;
 }

 const nav:ContentNavItem[]=[{id:'content-identity',label:'Identidade'},{id:'content-proposal',label:'Proposta'},...(trainer?[{id:'content-method',label:'Método'}]:[]),...definitions.map(def=>({id:`content-${def.key}`,label:def.label})),{id:'content-contact',label:'Contato'}];
 return <div className="editor-page"><header><span className="eyebrow dark-text">CONTEÚDO</span><h1>Edite o site por seções.</h1><p>Os campos seguem a ordem da página e permanecem independentes do modelo visual.</p></header>{saved?<div className="notice success">Alterações salvas no rascunho.</div>:null}
  <ContentWorkspace slug={project.slug} previewUrl={previewUrl} saved={Boolean(saved)} portfolio={false} nav={nav} action={saveContentAction}>
   <EditorSection id="content-identity" number="01" title="Identidade" summary="Nome, localização e apresentação" open><div className="form-grid"><label className="field"><span>Nome profissional</span><input name="name" defaultValue={v(data.identity,'name')||project.name}/></label><label className="field"><span>Cidade / região</span><input name="location" defaultValue={v(data.identity,'location')}/></label></div><label className="field"><span>Apresentação em uma frase</span><input name="tagline" defaultValue={v(data.identity,'tagline')}/></label><label className="field"><span>Sobre você</span><textarea name="description" rows={3} defaultValue={v(data.identity,'description')}/></label></EditorSection>
   <EditorSection id="content-proposal" number="02" title="Proposta" summary="Mensagem principal, oferta e botões"><label className="field"><span>Título principal</span><textarea name="hero_title" rows={2} defaultValue={v(data.content,'hero_title')}/></label><label className="field"><span>Resumo da proposta</span><textarea name="hero_text" rows={2} defaultValue={v(data.content,'hero_text')}/></label><label className="field"><span>Serviço principal</span><textarea name="primary_offer" rows={3} defaultValue={v(data.content,'primary_offer')}/></label><label className="field"><span>Diferencial</span><textarea name="proof" rows={3} defaultValue={v(data.content,'proof')}/></label><label className="field"><span>Sobre o trabalho</span><textarea name="about" rows={4} defaultValue={v(data.content,'about')}/></label><div className="form-grid"><label className="field"><span>Botão principal</span><input name="cta_primary" defaultValue={v(data.content,'cta_primary')}/></label><label className="field"><span>Botão secundário</span><input name="cta_secondary" defaultValue={v(data.content,'cta_secondary')}/></label></div></EditorSection>
   {trainer?<EditorSection id="content-method" number="03" title="Resultados e método" summary="Especialidade, registro e provas"><div className="form-grid"><label className="field"><span>Especialidade</span><input name="trainer_specialty" defaultValue={v(data.content,'trainer_specialty')}/></label><label className="field"><span>CREF</span><input name="trainer_cref" defaultValue={v(data.content,'trainer_cref')}/></label></div><label className="field"><span>Resumo do método</span><textarea name="system_intro" rows={3} defaultValue={v(data.content,'system_intro')}/></label><label className="field"><span>Resumo dos resultados</span><textarea name="results_intro" rows={3} defaultValue={v(data.content,'results_intro')}/></label></EditorSection>:null}
   <RepeatableSections definitions={definitions} initial={data.content} collapsible numberOffset={trainer?4:3}/>
   <EditorSection id="content-contact" number={String(definitions.length+(trainer?4:3)).padStart(2,'0')} title="Contato" summary="Canais e informações de atendimento"><div className="form-grid"><label className="field"><span>WhatsApp</span><input name="whatsapp" defaultValue={v(data.contact,'whatsapp')}/></label><label className="field"><span>Telefone</span><input name="phone" defaultValue={v(data.contact,'phone')}/></label><label className="field"><span>E-mail</span><input name="email" type="email" defaultValue={v(data.contact,'email')}/></label><label className="field"><span>Instagram</span><input name="instagram" defaultValue={v(data.contact,'instagram')}/></label></div><label className="field"><span>Endereço / local de atendimento</span><input name="address" defaultValue={v(data.contact,'address')}/></label><label className="field"><span>Horários ou observações</span><textarea name="hours" rows={2} defaultValue={v(data.contact,'hours')}/></label></EditorSection>
   <input type="hidden" name="extra_notes" value={v(data.content,'extra_notes')}/>
  </ContentWorkspace>
 </div>;
}
