import { resolveProjectAccess } from '@/core/session';
import { readProjectState } from '@/core/publishing';
import { saveSettingsAction } from '../actions';

export default async function SettingsPage({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{saved?:string}>}){const {slug}=await params,{saved}=await searchParams,{project}=await resolveProjectAccess(slug),state=await readProjectState(project.id);return <div className="editor-page"><header><span className="eyebrow dark-text">CONFIGURAÇÕES</span><h1>Endereço e base técnica.</h1><p>Subdomínio WebAppCap é nativo; domínio próprio entra em validação separada.</p></header>{saved&&<div className="notice success">Configurações salvas.</div>}<form action={saveSettingsAction} className="form-stack"><input type="hidden" name="slug" value={project.slug}/><section className="editor-section"><h2>Domínio</h2><div className="native-domain"><span>Subdomínio WebAppCap</span><div><input name="nativeSubdomain" defaultValue={state?.native_subdomain||project.slug}/><strong>.webappcap.com.br</strong></div><small>Fica ativo automaticamente quando salvo.</small></div><label className="field"><span>Domínio próprio</span><input name="customDomain" defaultValue={state?.custom_domain||''} placeholder="meusite.com.br"/><small>Status atual: {state?.domain_status||'unconfigured'}</small></label></section><section className="editor-section"><h2>Estrutura protegida</h2><p className="help">Template, segmento, permissões e renderer não são alterados aqui. Troca de template ficará em uma área própria quando houver mais de um modelo disponível para o segmento.</p></section><div className="sticky-save"><button className="action primary">Salvar configurações</button></div></form></div>}
