import { resolveProjectAccess } from '@/core/session';
import { readProjectState } from '@/core/publishing';
import { saveSettingsAction, validateDomainAction } from '../actions';

export default async function SettingsPage({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{saved?:string;domain?:string}>}){const {slug}=await params,{saved,domain}=await searchParams,{project}=await resolveProjectAccess(slug),state=await readProjectState(project.id);return <div className="editor-page"><header><span className="eyebrow dark-text">CONFIGURAÇÕES</span><h1>Endereço e base técnica.</h1><p>Subdomínio WebAppCap é nativo; domínio próprio passa por validação DNS antes de ficar público.</p></header>{saved&&<div className="notice success">Configurações salvas.</div>}{domain==='active'&&<div className="notice success">Domínio validado e ativado.</div>}{domain==='pending'&&<div className="notice">DNS ainda não validado. Confira o apontamento e tente novamente.</div>}{domain==='missing'&&<div className="notice">Salve um domínio próprio antes de validar.</div>}<form action={saveSettingsAction} className="form-stack"><input type="hidden" name="slug" value={project.slug}/><section className="editor-section"><h2>Domínio</h2><div className="native-domain"><span>Subdomínio WebAppCap</span><div><input name="nativeSubdomain" defaultValue={state?.native_subdomain||project.slug}/><strong>.webappcap.com.br</strong></div><small>Fica disponível automaticamente para projetos publicados.</small></div><label className="field"><span>Domínio próprio</span><input name="customDomain" defaultValue={state?.custom_domain||''} placeholder="meusite.com.br"/><small>Status atual: {state?.domain_status||'unconfigured'}</small></label></section><section className="editor-section"><h2>Estrutura protegida</h2><p className="help">Template, segmento, permissões e renderer não são alterados aqui. Troca de template ficará em uma área própria quando houver mais de um modelo disponível para o segmento.</p></section><div className="sticky-save"><button className="action primary">Salvar configurações</button></div></form>{state?.custom_domain&&state.domain_status!=='active'&&<form action={validateDomainAction} className="form-stack"><input type="hidden" name="slug" value={project.slug}/><section className="editor-section"><h2>Validar domínio próprio</h2><p className="help">Depois de configurar o DNS do domínio, execute a verificação. O endereço só passa a servir o site quando a validação for concluída.</p><button className="action">Verificar DNS agora</button></section></form>}</div>}
