import { resolveProjectAccess } from '@/core/session';
import { readV2Content } from '@/core/onboarding-data';
import { uploadMediaAction } from '../actions';

const url=(o:Record<string,unknown>,k:string)=>{const x=o[k];return x&&typeof x==='object'&&'url' in x?String((x as {url?:unknown}).url||''):''};
export default async function MediaPage({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{saved?:string}>}){const {slug}=await params,{saved}=await searchParams,{project}=await resolveProjectAccess(slug),data=await readV2Content(project.id);const gallery=Array.isArray(data.media.gallery)?data.media.gallery:[];const portfolio=project.segment==='portfolio',fitness=project.segment==='personal-trainer',logo=url(data.media,'logo')||(portfolio?url(data.media,'profile'):''),heroVideo=url(data.media,'hero_video');return <div className="editor-page"><header><span className="eyebrow dark-text">MÍDIA</span><h1>Biblioteca visual.</h1><p>Gerencie a mídia usada pelos modelos. Salvar atualiza somente o rascunho; publique depois de conferir no Preview.</p></header>{saved&&<div className="notice success"><strong>Mídia salva apenas no rascunho.</strong> Confira no Preview e publique quando estiver tudo certo.</div>}<form action={uploadMediaAction} className="form-stack"><input type="hidden" name="slug" value={project.slug}/><section className="editor-section"><div className="upload-grid"><label className="upload-card"><strong>{portfolio?'Foto de perfil / logo':'Logo'}</strong>{logo&&<img src={logo} alt={portfolio?'Foto de perfil atual':'Logo atual'}/>}<span>Enviar nova imagem</span><input name="logo" type="file" accept="image/jpeg,image/png,image/webp,image/gif"/></label><label className="upload-card"><strong>Imagem principal / poster</strong>{url(data.media,'hero')&&<img src={url(data.media,'hero')} alt="Imagem principal atual"/>}<span>Enviar nova imagem</span><input name="heroImage" type="file" accept="image/jpeg,image/png,image/webp,image/gif"/></label></div>{fitness&&<label className="upload-card"><strong>Vídeo do hero</strong><span>{heroVideo?'Vídeo atual configurado. Envie outro para substituir.':'Opcional. Se vazio, o site usa a imagem principal.'}</span>{heroVideo&&<video src={heroVideo} controls preload="metadata" style={{width:'100%',maxHeight:320,borderRadius:12}}/>}<small>MP4 ou WebM, até 50 MB. Use vídeo curto, sem áudio essencial. A imagem principal funciona como poster/fallback.</small><input name="heroVideo" type="file" accept="video/mp4,video/webm"/></label>}<label className="upload-card"><strong>Adicionar à galeria</strong><span>Selecione várias imagens. O CMS mantém até 12 e os templates usam as imagens conforme o layout.</span><input name="gallery" type="file" accept="image/jpeg,image/png,image/webp,image/gif" multiple/></label>{gallery.length>0&&<div className="media-gallery">{gallery.map((item,i)=>{const src=item&&typeof item==='object'&&'url' in item?String((item as {url?:unknown}).url||''):'';return src?<img key={`${src}-${i}`} src={src} alt={`Galeria ${i+1}`}/>:null})}</div>}</section><div className="sticky-save"><button className="action primary">Salvar mídia no rascunho</button></div></form></div>}
